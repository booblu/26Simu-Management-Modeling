# 🚨 全局核心环境指令 (Global AI Directives)

- **绝对物理路径守则**：自 2026 春季学期起，本门课程的唯一绝对合法物理路径与工程命名为 **`26管理建模与仿真`**（以及远端对应的 `26Simu-Management-Modeling`）。
- **违禁词拦截**：在任何路径查找、文件命名、命令行读写中，**严禁使用和访问旧有的 `25` 级目录前缀**。所有的操作心智模型必须 100% 建立在 `26` 时空之上。
- **第一性原理**：处理本项目的任何问题、生成脚本或文件体系时，必须自动代入 `26` 版本的设定。
- **架构解耦铁律 (SSOT Architecture)**：主教学库（Syllabus/Wiki）严禁混放学生的初始作业模板代码（Starter Repos）。任何需要通过 GitHub Classroom 下发给学生的实战沙盒代码骨架，必须被建立为独立且纯净的平行仓库（如 `booblu/26Simu-Project1-Starter`），并只通过 URI 指引学生查阅主库的作业要求文档。不要“代劳”帮学生写好机制推理与物理层核心方程。
